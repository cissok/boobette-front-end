export default defineNuxtRouteMiddleware(async (to, from) => {
  console.log('middleware/subscription.ts')
  // const authStore = useAuthStore()
  // if (!authStore.user) {
  //   return navigateTo('/login')
  // }
})